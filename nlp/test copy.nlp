宣言: voidを返す関数mainを 引数無し 処理「
    引数100で関数runを呼び出したものの値を放置する。
」とする。

宣言: voidを返す関数runを 引数int:max 処理「
    宣言: intの変数xを用意する。
    宣言: intの変数yを用意する。
    宣言: intの変数zを用意する。
    1の値を変数xへ代入する。
    1の値を変数yへ代入する。
    1の値を変数zへ代入する。
    条件「xがmaxより小さい」が真の間、処理「
        引数xで関数outを呼び出したものの値を放置する。
        yとxの和の値を変数zへ代入する。
        yの値を変数xへ代入する。
        zの値を変数yへ代入する。
    」を繰り返す。
」とする。